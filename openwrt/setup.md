# install docker

to run compose use

`docker-compose up -d`

# vpn traffic through wireguard client with tailscale exit node

- install wireguard client with your VPN provider
- install dockerd + docker-compose packages

# proton vpn setup

https://protonvpn.com/support/openwrt-wireguard

# tailscale + vpn

- install dockerd, docker-compose
- create container using compose with host mode [put link to compose example]
- do tailscale authentication
